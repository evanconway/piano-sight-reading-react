import "react";

const Options = () => {
    

    return <button style={{
        position: "absolute",
        fontSize: "large",
        margin: 10,
        opacity: 1,
    }}>Options</button>;
};

export default Options;
